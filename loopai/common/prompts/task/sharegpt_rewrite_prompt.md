### Task
Rewrite the raw record into ShareGPT-style SFT training data.

### Target schema / format requirements
{target_schema}

### Benchmark example (follow structure and style)
{benchmark_example}

### Raw record to convert (JSON)
{raw_record}

Return only the rewritten single JSON object.